# Backend

Month 1 contains only a minimal FastAPI health endpoint.

Run from the project root:

```bash
uvicorn backend.app.main:app --reload
```

Open `/docs` in the local browser to inspect the generated OpenAPI documentation.
